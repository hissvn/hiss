(setq modules (map (split-lines (get-content "aoc/input1-1.txt")) parse-int))
(defun fuel (module)
    (setlocal number (haxe- (floor (haxe/ module 3)) 2))
    (if (haxe< number 0) 
        0
        (+ number (fuel number))))
(print (sum (map modules fuel)))